<Window x:Class="SIL.Cog.Views.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SIL.Cog.ViewModels" mc:Ignorable="d"
        Title="{Binding Value, StringFormat={}{0}% completed}" ShowInTaskbar="False"
		WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Loaded="Window_Loaded" SizeToContent="WidthAndHeight"
        d:DataContext="{d:DesignInstance vm:ProgressViewModel}">
    <StackPanel Margin="10" >
        <TextBlock Margin="0,0,0,5" Text="{Binding Text}" />
        <TextBlock Margin="0,0,0,5" Text="{Binding TimeRemaining}" />
        <ProgressBar Name="ProgressBar" Value="{Binding Value}" Margin="0,0,0,5" Width="250" Height="20" />
        <Button HorizontalAlignment="Right" Command="{Binding CancelCommand}" Width="70">Cancel</Button>
    </StackPanel>
</Window>
