<UserControl x:Class="SIL.Cog.Views.HierarchicalGraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:zoom="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions"
             xmlns:Views="clr-namespace:SIL.Cog.Views" xmlns:ViewModels="clr-namespace:SIL.Cog.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <zoom:ZoomControl ZoomBoxOpacity="0.5">
        <Views:HierarchicalGraphLayout Graph="{Binding Graph}" LayoutAlgorithmType="Tree" OverlapRemovalAlgorithmType="FSA" HighlightAlgorithmType="Simple">
            <Views:HierarchicalGraphLayout.Resources>
                <DataTemplate DataType="{x:Type ViewModels:HierarchicalGraphVertex}">
                    <Grid>
                        <TextBlock Name="nameText" Text="{Binding Name}" />
                        <Ellipse Name="circle" Width="5" Height="5" Fill="Black" Visibility="Collapsed" />
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsCluster}" Value="True">
                            <Setter TargetName="nameText" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="circle" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Views:HierarchicalGraphLayout.Resources>
        </Views:HierarchicalGraphLayout>
    </zoom:ZoomControl>
</UserControl>
