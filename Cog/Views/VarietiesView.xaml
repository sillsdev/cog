<UserControl x:Class="SIL.Cog.Views.VarietiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Views="clr-namespace:SIL.Cog.Views"
             xmlns:Services="clr-namespace:SIL.Cog.Services" mc:Ignorable="d" Services:ViewRegistrationService.IsRegisteredView="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBlock Margin="5" DockPanel.Dock="Top" Style="{StaticResource ViewTitleStyle}" Text="Variety details" />
            <ComboBox Margin="5" FontSize="16" ItemsSource="{Binding Varieties}" SelectedItem="{Binding CurrentVariety}" DisplayMemberPath="Name" />
        </StackPanel>
        <Views:VarietyView DataContext="{Binding CurrentVariety}" />
    </DockPanel>
</UserControl>
