<UserControl x:Class="SIL.Cog.Views.WordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Views="clr-namespace:SIL.Cog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate x:Key="blankTemplate" />
    </UserControl.Resources>
    <ListBox BorderThickness="0" x:Name="_listBox" Background="Transparent" FontSize="16" ItemsSource="{Binding Segments}" Views:ItemsControlDragDrop.DragDropTemplate="{StaticResource blankTemplate}">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Padding" Value="3" />
            </Style>
        </ListBox.ItemContainerStyle>
    </ListBox>
</UserControl>
