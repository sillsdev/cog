<UserControl x:Class="SIL.Cog.Views.VarietyPairsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Views="clr-namespace:SIL.Cog.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBlock Margin="0,0,5,0" DockPanel.Dock="Top" Style="{StaticResource ViewTitleStyle}" Text="Variety pair details" />
            <ComboBox Margin="5,0,0,0" FontSize="16" ItemsSource="{Binding Varieties}" SelectedItem="{Binding CurrentVariety1}" DisplayMemberPath="Name" />
            <ComboBox Margin="5,0,0,0" FontSize="16" ItemsSource="{Binding Varieties}" SelectedItem="{Binding CurrentVariety2}" DisplayMemberPath="Name" />
        </StackPanel>
        <Views:VarietyPairView DataContext="{Binding CurrentVarietyPair}" />
    </DockPanel>
</UserControl>
