<UserControl x:Class="SIL.Cog.Views.TaskAreaCommandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SIL.Cog.ViewModels" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance vm:TaskAreaCommandsViewModel}">
    <HeaderedContentControl Margin="8,20,8,0" Header="{Binding DisplayName}" Style="{StaticResource TaskAreaHeaderStyle}">
        <ItemsControl ItemsSource="{Binding Commands}">
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type vm:CommandViewModel}">
					<TextBlock Margin="0,3,0,0" TextWrapping="Wrap">
                        <Hyperlink Style="{StaticResource TaskAreaHyperlinkStyle}" Command="{Binding Command}">
                            <Run Text="{Binding DisplayName, Mode=OneWay}" />
                        </Hyperlink>
					</TextBlock>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
    </HeaderedContentControl>
</UserControl>
